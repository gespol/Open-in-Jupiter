// background.js - Script de Plano de Fundo (Service Worker) da extensão
chrome.runtime.onInstalled.addListener(() => {
  console.log("Solana Jupiter Address Detector: extensão instalada/ativada.");
});

// (Nenhuma outra lógica de background é necessária para esta extensão específica)